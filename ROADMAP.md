# 博客功能路线图（Roadmap）

> 目标：把现有「内容发布型博客」逐步升级为「内容 + 社交 + 聚合」的轻量个人门户。
> 现状基线、模块拆分、分期计划、技术要点与风险都列在下面。本文档只规划，不含代码实现。

---

## 一、现状基线（已实现）

| 能力 | 说明 |
|------|------|
| 内容 | 文章 CRUD（后台）、分类、标签、Markdown 渲染（已 bleach 清洗） |
| 互动 | 评论（带 IP 属地 + 设备）、点赞（前端 localStorage 去重） |
| 社交 | 友情链接（仅展示链接）、用户注册/登录、三级角色（super/admin/user） |
| 阅读 | 列表分页、搜索（LIKE）、归档时间线、天气组件 |
| 运营 | 访问统计（区域/时段/热读/热搜）、RSS/fee.xml、sitemap、robots |
| 安全 | 关 debug、密钥强制环境变量、CORS 同源、限流、开放重定向修复 |

**两大缺口**：① 无「短动态/留言墙/互动通知」类社交内容；② 友链未聚合外部内容。

---

## 二、功能地图（按模块）

### 模块 A：社交聚合（「广场」页）★ 你最想要的
- **A1 微动态 Moment**：发短句/随手记，不写长文。
- **A2 友链 RSS 聚合（博客圈）**：给 `FriendLink` 加 `rss_url`，后台定时抓取友链站点 RSS，按时间混排成聚合流。
- **A3 社交账号墙**：GitHub / B站 / 知乎 / 微博 入口，访客一键前往关注。
- **A4 动态互动**：动态点赞、评论、@ 通知。

技术要点：
- 新增模型 `Moment`（id, author_id, content, created_at, likes, images?）；`FriendLink.rss_url` 扩字段。
- 接口：`/api/moments`（列表）、`/api/moment`（作者发布）、`/api/moment/<id>/like`、`/api/feed/circle`（友链 RSS 混排）。
- 前端：新增 `SquareView.vue`，复用现有 `Sidebar`/`PostCard` 样式。
- 依赖：`feedparser`（宝塔终端 `pip install`，同 bleach 流程）；RSS 抓取务必**异步 + 缓存**，避免阻塞请求；抓取的外部 HTML 同样走 `clean_html()` 防 XSS。

### 模块 B：内容与阅读体验
- **B1 相关文章推荐**：按标签重合度算相似度，纯算法零依赖。
- **B2 热门排行 widget**：复用 `VisitLog` / `ReadLog` 出「本周热门」。
- **B3 文章目录 TOC + 阅读进度条**：长文体验提升明显。
- **B4 文章系列 / 专栏**：多篇成系列，带上下篇导航。
- **B5 全文搜索升级**：SQLite FTS5 替代 LIKE（数据量大时才有感）。

### 模块 C：互动与社区
- **C1 留言墙 Guestbook**：独立于文章评论的短留言页。
- **C2 评论嵌套回复 + @ 通知**：当前是平铺评论，需加 `parent_id` 与回复提醒。
- **C3 邮件订阅 / Newsletter**：RSS + 邮箱，运营留量（需邮件发送能力，可用 SMTP 或第三方）。

### 模块 D：运营与分发
- **D1 一键分享卡片（OG 标签）**：完善 Open Graph，利于微信/微博转发。
- **D2 微信 / Telegram 新文推送**：需对应机器人/接口。
- **D3 Webhook 自动部署**：GitHub push → 宝塔拉取，省去手动传包（需宝塔 Webhook 或脚本）。
- **D4 站点公告 / 置顶动态**：首页顶部可关闭的公告条。

---

## 三、分期计划（路线图）

| 阶段 | 内容 | 工作量 | 依赖 | 收益 |
|------|------|--------|------|------|
| **Phase 1 · 热身 MVP** | B1 相关文章、B2 热门排行、B3 TOC+进度条、A1 微动态基础 | S~M | 无外部依赖 | 零风险见效快，熟悉新增表/接口/页面的完整链路 |
| **Phase 2 · 社交核心** | A2 友链 RSS 聚合、A3 社交账号墙、A4 动态互动、C1 留言墙 | M~L | feedparser | 真正落地「社交聚合页」 |
| **Phase 3 · 互动深化** | C2 评论嵌套+@、C3 邮件订阅、B4 文章系列 | M~L | 邮件发送能力 | 社区感与留存提升 |
| **Phase 4 · 运营分发** | D1 分享卡片、D2 推送、D3 自动部署、D4 公告、B5 搜索升级 | M（零散） | 各平台权限 | 降低运维成本、扩大传播 |

> 工作量标注：S = 半天内；M = 1~2 天；L = 3 天以上（含联调与测试）。

---

## 四、技术注意与风险

1. **数据库变更**：当前用 `db.create_all()` 自动建表。新增模型会自动建；但**给已有表加字段（如 `FriendLink.rss_url`）不会自动迁移**，需手动 `ALTER TABLE` 或引入 Alembic。上线前务必在宝塔备份数据库。
2. **安全**：微动态 / 留言墙内容同样要走 `clean_html()` 清洗 + 限流；RSS 抓取注意防 SSRF（只抓 http/https、限制内网地址）。
3. **性能**：RSS / 外部接口一律异步 + 缓存（可复用你已有的 `IpRegion` 缓存思路），前端展示用缓存结果，不实时抓取。
4. **增量上线**：每做一个功能就重打部署包、走你已建立的「安全审计 → 宝塔覆盖 → 强刷」流程。
5. **开源同步**：llhhy-blog 是唯一仓库，新功能在哪个分支开发需提前约定（建议 `dev` 分支，稳定后合 `main` 再发 Release）。

---

## 五、建议执行顺序

作为零基础起步，**强烈建议从 Phase 1 开始**：B1/B2/B3 零新依赖，能完整跑通「加模型/接口 → 前端页面 → 部署」的最小闭环，建立信心；再上 A1 微动态（第一次写发布接口）；最后进 Phase 2 做 RSS 聚合这块真正的「社交聚合」。

下一步：确认从 Phase 1 的哪个功能动手，我按该选择逐步带做（边写边讲解）。
