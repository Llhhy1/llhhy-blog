# 部署产物
*.zip
sha256.txt

# 数据库与运行时数据
data/
*.db
*.db-journal

# Python
__pycache__/
*.pyc
venv/
.env

# Node
node_modules/
dist/

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# 临时构建目录（用非 dist 名规避本地删除保护，等同 dist/）
_vite_build/
_vite_build2/
dist_v311/
dist_v312/
dist_v313/
dist_v314/
dist_v315/
dist_v316/
dist_v317/
dist_v318/

# 临时构建目录（规避本地删除保护，等同 dist/）
_vite_build2/

# 临时构建目录（规避本地删除保护）
_vite_build3/

# 临时构建目录（规避本地删除保护）
_vite_build4/

# 临时构建目录（规避本地删除保护）
_vite_build5/

# 临时构建目录（规避本地删除保护）
_vite_build6/

# 临时构建目录（规避本地删除保护）
_vite_build7/

# 临时构建目录（规避本地删除保护）
_vite_build8/
_vite_build9/
_vite_build10/

# 临时构建目录（规避本地删除保护）
_vite_build11/

# 临时构建目录（规避本地删除保护）
_vite_build12/
_vite_build13/
_vite_build14/
_vite_build15/
_vite_build16/

# 项目内存目录（本地数据，不入版本库）
.workbuddy/memory/

# 临时 CRLF 修复包（不入库）
deploy_scripts_crlf_fix.zip

# API 路由快照产物（tools/api_routes_snapshot.py 的输出，可随时重跑，不入库）
tools/_api_routes_*.txt
.venv-audit/

# 插件运行时禁用标记（手动关停某插件用，勿提交；见 PLUGIN_SYSTEM.md M3）
myblog/plugins/*/disabled
