# 部署产物
*.zip

# 数据库与运行时数据
data/
*.db
*.db-journal

# Python
__pycache__/
*.pyc
venv/
.env

# Node
node_modules/
dist/

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# 临时构建目录（用非 dist 名规避本地删除保护，等同 dist/）
_vite_build/

# 临时构建目录（规避本地删除保护，等同 dist/）
_vite_build2/

# 临时构建目录（规避本地删除保护）
_vite_build3/

# 临时构建目录（规避本地删除保护）
_vite_build4/
