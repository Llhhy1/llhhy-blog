# 部署产物
*.zip

# 数据库与运行时数据
data/
*.db
*.db-journal

# Python
__pycache__/
*.pyc
venv/
.env

# Node
node_modules/
dist/

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# 临时构建目录（用非 dist 名规避本地删除保护，等同 dist/）
_vite_build/
_vite_build2/
dist_v311/

# 临时构建目录（规避本地删除保护，等同 dist/）
_vite_build2/

# 临时构建目录（规避本地删除保护）
_vite_build3/

# 临时构建目录（规避本地删除保护）
_vite_build4/

# 临时构建目录（规避本地删除保护）
_vite_build5/

# 临时构建目录（规避本地删除保护）
_vite_build6/

# 临时构建目录（规避本地删除保护）
_vite_build7/

# 临时构建目录（规避本地删除保护）
_vite_build8/
_vite_build9/
_vite_build10/

# 临时构建目录（规避本地删除保护）
_vite_build11/

# 临时构建目录（规避本地删除保护）
_vite_build12/
_vite_build13/

_vite_build14/

_vite_build14/
